namespace WebShop.Models
{
    /// <summary>
    /// Enum for status
    /// </summary>
    public enum Status
    {
        Created,
        Paid,
        Sending,
        Cancelled,
        Completed
    }
}